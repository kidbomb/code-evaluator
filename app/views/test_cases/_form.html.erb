<%= form_for([@test_case.problem, @test_case]) do |f| %>
  <% if @test_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_case.errors.count, "error") %> prohibited this test_case from being saved:</h2>

      <ul>
      <% @test_case.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :input %><br>
    <%= f.text_area :input %>
  </div>
  <div class="field">
    <%= f.label :expected_output %><br>
    <%= f.text_area :expected_output %>
  </div>
  <div class="actions">
    <%= f.submit "Create test case", :class=>"btn btn-primary" %>
  </div>
<% end %>
